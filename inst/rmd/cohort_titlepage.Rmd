---
params:
  cohort_defs: NA
  analysis_description: NA
  dataset_description: NA
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
library(dplyr)
library(lubridate)

```

# Cohort Analysis Overview
`r params$analysis_description`

## Analysis Details
```{r report_details}

# # Strip patient lists, if included
# params_combo <- params
# params_combo$cohort_defs <- lapply(params_combo$cohort_defs, function(x) {x$df_patient_list <- NULL; x})

report_analysis_info_table(Sys.time())  # paste0(params_combo, collapse = "-"))
```

## Base Dataset
```{r dataset_definitions}
report_dataset_info_table(params$dataset_description)
```

## Cohort Definitions
```{r cohort_definitions}
report_cohort_info_table(params$cohort_defs)
```
